import { FormControlName, FormGroup } from "@angular/forms";
import { lookupService } from "dns";



export function confirmedValidator(FormControlName: string, matchingControlName: string) {
    return (formValidar: FormGroup) =>{

    }
}